# No se necesitan variables de entorno adicionales.
# Las credenciales OAuth de Google (solo Search Console) se leen desde:
#   credentials.json  — client_id / client_secret
#   token.json        — access_token / refresh_token
#
# Ambos archivos se copian desde:
#   D:\Gerencia\Desktop\Vitaliah desarrollos digitales\Dashboards\gsc-mcp-server\
#
# El calendario de artículos vive en content-calendar.yaml (raíz del repo),
# no en Google Sheets -- collect.js y calendar.js lo leen/escriben directamente.
